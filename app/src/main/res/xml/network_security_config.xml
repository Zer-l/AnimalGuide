<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release：仅允许 HTTPS，禁止明文传输 -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug：允许 localhost 明文（方便本地调试代理） -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
